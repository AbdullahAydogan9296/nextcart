'use client';

import React from 'react';

const ProductDescription = ({ description }) => {
    return (
        <p className="mt-12 text-gray-600 mb-16">{description}</p>
    );
};

export default ProductDescription;